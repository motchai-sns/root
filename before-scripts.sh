sudo mkdir -p data-mount/local_pgdata
sudo mkdir -p data-mount/local_pgadmin-data
sudo mkdir -p data-mount/elasticsearch/data

sudo chmod -R 777 ./data-mount/local_pgdata
sudo chmod -R 777 ./data-mount/local_pgadmin-data
sudo chmod -R 777 ./data-mount/elasticsearch/data
